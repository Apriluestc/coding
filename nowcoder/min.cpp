#include <bits/stdc++.h>

using namespace std;

/*
 * 旋转数组的最小元素
 *
 * 把一个数组最开始的若干元素搬到数组的末端，我们称之为数组的旋转，
 * 输入一个递增排序数组的一个旋转，输出旋转数组的最小元素
 * 例如：数组 {3, 4, 5, 2, 1} 为 {1, 2, 3, 4, 5} 的一个旋转，
 * 该数组的最小值为 1
 * */

class Solution {
public:
    static int min(vector<int>& nums, int size) {
        if (nums.empty()) {
            return -1;
        }

    }
};

void test() {}

int main()
{
    test();
    return 0;
}
