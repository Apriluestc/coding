#include <bits/stdc++.h>

using namespace std;

/*
 * 定义栈的数据结构，请在该类中实现一个能够得到栈的最小元素 min，
 * 在该栈中，调用 min，push，pop 的时间复杂度都是 O(c)
 * */

class MinStack {
public:
    void push();
    void pop();
    int top();
    int getMin();
};

int main()
{
    return 0;
}
